# Evaluation

The final golden set is human-verified.

AI-assisted labels are provisional and must not be described as human ground truth.
The frozen golden set must not be used for model tuning or retrieval-index construction.
